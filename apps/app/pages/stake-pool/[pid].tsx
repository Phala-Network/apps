import PageHeader from '@/components/PageHeader'
import {Container} from '@mui/material'
import {FC} from 'react'

const StakePool: FC = () => {
  return (
    <Container maxWidth="lg">
      <PageHeader title="Stake Pool"></PageHeader>
    </Container>
  )
}

export default StakePool
