{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["out/**", ".next/**"],
      "env": ["CONTEXT", "NODE_ENV"]
    },
    "lint": {"outputs": []},
    "lint:fix": {"outputs": []}
  }
}
