{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [
        "out/**",
        ".next/**",
        "build/**",
        ".svelte-kit/**",
        ".vercel/**"
      ],
      "env": ["NODE_ENV", "VERCEL_ENV"]
    }
  }
}
