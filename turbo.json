{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["out/**"]
    },
    "@phala/app#build": {
      "dependsOn": ["^build"],
      "outputs": ["public/**"]
    },
    "lint": {"outputs": []},
    "lint:fix": {"outputs": []}
  }
}
